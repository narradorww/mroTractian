import * as React from 'react';
import { View, Text } from 'react-native'

export default function Companies () {
    return (
        <View>
            <Text>Companies</Text>
        </View>
    );
};